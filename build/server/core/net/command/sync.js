// Generated by CoffeeScript 1.9.3
Net.command.sync = {
  full: function(socket) {
    var buffer, world;
    world = Serializer.serialise(Engine.world);
    buffer = new Buffer(world.length + 1);
    buffer[0] = Net.mask('fullSync');
    buffer.write(world, 1);
    socket.send(buffer, {
      binary: true
    });
    return socket.isFirstSyncWas = true;
  },
  diff: function(socket) {
    var world;
    return world = Serializer.serialise(Engine.world);
  },
  lastDiffTime: null
};
